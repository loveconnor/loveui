export default function CheckoutPage() {
  return (
    <main className="mx-auto flex min-h-screen max-w-xl flex-col justify-center px-6">
      <h1 className="text-3xl font-semibold tracking-tight">Get access</h1>
      <p className="mt-3 text-muted-foreground">
        Connect this page to the checkout provider, then issue an access token
        for CLI registry installs.
      </p>
    </main>
  );
}
